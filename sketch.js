function setup() {
  // Create a canvas that fills the entire browser window
  createCanvas(500, 500);
}
// the person reading this code is gay
//har har har har
function draw() {
background(200,120,200);

circle(250,250,450);
}
